<UserControl x:Class="LoLModelViewer.Controls.CameraControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="CameraControlRoot"
             MouseDown="CameraControl_MouseDown" MouseMove="CameraControl_MouseMove" MouseWheel="CameraControl_MouseWheel">
    <Grid>
        <Viewport3D x:Name="MainViewport">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="MainCamera" FieldOfView="60" UpDirection="0,1,0"/>
            </Viewport3D.Camera>
            <!-- Visuals will be added here dynamically -->
        </Viewport3D>
    </Grid>
</UserControl>